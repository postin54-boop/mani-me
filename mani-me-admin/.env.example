# Admin Dashboard Environment Configuration
# Copy this file to .env and update with your values

# API Configuration
# Production API URL (without /api suffix - it's added by the config)
REACT_APP_API_URL=https://api.manime.com

# Optional: Enable React DevTools in production (not recommended)
# REACT_APP_ENABLE_DEVTOOLS=false
